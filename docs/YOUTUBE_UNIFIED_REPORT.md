# 유튜브 통합 레포트 운영

2026-09-20 사용자 확정. 적용일은 2026-09-21 KST.

프롬프트 정본은 `prompts/youtube_investment_trends.md`다. 전체 요약 3~5줄,
핵심 흐름 3~5개, 마지막 “그래서 한마디로?” 2~3문장으로 구성한다.
근거·원인·기업이 언급된 이유·의견 차이·사업 진행 단계를 설명한다.
약 1,500자 시안의 약 1.5배를 기준으로 하고 이전 날짜 보고서 길이에 배수를 적용하지 않는다.
개별 출처 링크, 영상별 나열, 채널별 토글, 표를 생성하지 않는다.

## 실행 경로

GitHub Actions의 `youtube_report.py`는 기존 영상 분석을 캐시에 저장한다.
9/21부터 영상별 노션 보고서 블록 생성/업로드를 중단한다. 기존 JSON 소비자는 유지한다.

노트북의 기존 `com.vinicius.daily-archive` 감시자가 당일 자료를 동기화한다.
유튜브 입력이 아직 없으면 생성 시도를 소비하지 않고 기다린다.
`daily_archive.collect`가 `youtube_digest.generate`를 통해 그날 글을 한 번 만든다.
대시보드 유튜브 슬롯에 게시한 원문을 다시 읽고, 종합추천 입력과 일일 Report 사본에 사용한다.
휴장일 별도 유튜브 LLM 브리핑은 생성하지 않는다. 새 스케줄/서비스는 추가하지 않는다.

## 비용과 실패

거래일은 당일 저장 분석본, 휴장일은 당일 포함 최근 3일만 사용한다.
중복 영상 ID를 제거하고 입력 전체가 10만자를 넘으면 자동 분할/추가 호출 없이 실패한다.
gpt-5.4-mini low, 응답 상한 6,000토큰, 호출당 120초, SDK 재시도 0회.
프롬프트·입력·모델 해시가 같으면 비공개 캐시를 재사용한다.
실패/결과 불명 호출은 자동 재요청하지 않는다. 노션 재게시에는 캐시된 결과를 사용한다.
생성 실패는 기존 아카이브 상태와 대시보드 경고에 표시한다.

입력/생성/사용량 감사 기록은 `.recommendation_audits/youtube-digest-*`다.
비공개 입력과 실계좌 정보는 저장소에 게시하지 않는다.

## 검증

`test_youtube_digest`는 날짜·중복 제거·입력 상한·캐시·실패 시 재호출 방지·노션 대조·
휴장일 중복 생성 방지·수집기의 채널별 업로드 중단을 격리 검증한다.
`test_recommendation_end_to_end`는 실제 내부 함수와 가짜 외부 응답으로
감시자 → 통합 글 → 종합추천 → 일일 페이지 → 게시 검증 경로를 검사한다.
검증에서 실제 모델 호출, 샘플 생성, 실제 노션 쓰기, 주문 실행은 하지 않는다.


## 2026-09-21 첫 운영 장애와 수정

16:21:39 일일 생성 시작, 16:22:18 후보 당일 수급 재조회 검증 실패로 전체 중단.
사전 검증과 생성이 수급을 각각 조회했고, 생성 실패 후 당일 자동 재실행은 차단됐다.
유튜브 통합 호출이 후보 수급 수집 뒤에 있어 영상 자료가 있어도 함께 차단된 것이 추가 원인이다.
당시 코드·원응답 진단을 저장하지 않아 개별 API 실패 원인은 소급 단정할 수 없다.

기존 60초 archive_watch에서 유튜브와 주식 아카이브를 독립된 작업으로 처리한다.
유튜브는 16:00~20:59에 입력을 10분 간격으로 동기화하며, 주식 생성 실패/완료 상태와 무관하다.
같은 기존 archive 잠금을 쓰며 별도 서비스는 없다. 새 영상이 늦게 추가돼도 당일 작성된 글은
다시 생성하지 않는다. 당일 receipt는 private youtube-daily-날짜.json에 보존한다.
주식 생성기는 사전 검증한 날짜·후보 일치 수급만 15분 이내 전달받아 재사용한다.
오래된 전달 값·누락·NaN·불리언·잘못된 후보는 거부한다. 검증을 완화하거나 0으로 채우지 않는다.

회귀 test_report_failure_isolation: 주식 실패 후에도 유튜브 게시, 중복 비용 방지,
수급 전달 검증, 생성 단계 수급 재조회 없음. 관련 122개 격리 테스트 통과.

복구 중 파미셀 검색 응답이 30일 이전인 8/12·8/7 발행 기사를 반환해 뉴스 검증에서도 중단됐다.
검증 범위를 완화하지 않고 프롬프트에 정확한 발행일 하한/상한을 추가했다.
해당 회사만 새로 조회해 통과시켰으며, 이미 통과한 6회사와 유튜브 글은 재사용했다.


## 2026-09-21 시장 동향 통합

사용자 추가 요청으로 오늘의 핵심요약/섹터 장세/유튜브를 한 글로 합친다. 별도 핵심요약과 보유 분석의 모델 호출은 폐지. 섹터 CSV 수집은 유지하고 독립 Notion 보고서와 차트 생성은 종료한다. 기존 영상별 분석 캐시를 재사용하며, 통합 모델은 1일 1회다. 다음 예약 실행부터 market_digest.sectors의 당일 완전한 수치가 있어야 통합 글을 생성한다. 휴장일은 명시된 휴장 캘린더에 따라 이전 거래일 섹터 날짜를 표시한다. 통합 글의 AI 의견과 실측 섹터 수치는 구별한다.


## 2026-09-21 후속: 그림과 표 복원

시장 동향 안에 기존 스타일의 키워드 트리맵·언급 맥락 표와 섹터 트리맵·4열 등락 표를 유지한다. 별도 보고서를 만드는 대신 market_visuals가 데이터로 렌더링한다. 추가 모델 호출은 없다. 동일한 영상 입력과 섹터 기준일을 사용하고, 숫자와 방향을 바꾸지 않는다. 캐시된 그림 파일 해시와 노션 이미지 캡션·표 셀을 검증하며 같은 게시 재호출 시 업로드/본문 추가가 발생하지 않는다. 차트용 시총·주도주 목록은 AI 프롬프트에서 제외한다. 9/21 기존 스냅샷에는 차트 부가 열이 없으므로 같은 날짜 수급 CSV에서 동일 유니버스/테마 분류의 시총·개별 등락을 보충했다. 이후에는 마감 수집 시 함께 기록한다.
